.form
  - @items.each do |item|
    .item-share
      = link_to item_path(item), class: "" do
        .item-share__image
          = image_tag item.item_images[0].image.url , alt: "商品画像", class: "",size: "187x187"
        .item-share__bottom
          %span.item-share__bottom__title
            =item.name
            .item-share__bottom__title__hidden-box
        .item-share__price
          ¥#{item.price} 

-# 配列の取り出し方各種
-# - @items_data.each do |items|
  -#   .item
  -#     = link_to "/", class: "" do
  -#       .item__image
  -#         = image_tag items[:image].image.url , alt: "商品画像", class: "",size: "187x187"
  -#       .item__bottom
  -#         %span.item__bottom__title
  -#           =items[:item].name
  -#           .item__bottom__title__hidden-box
  -#       .item__price
  -#         ¥#{items[:item].price} 
